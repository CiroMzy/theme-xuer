
{% comment %}

  Accepts:
  - id: {String} 
  - type: {String} 
  - name: {String} 
  - label: {String} 
  - required: {Boolean} 
  - className: {String} 
  - attr: {String} 
  - options: {Html} 
  - value: {String} 

{% endcomment %}


<xuer-form-select {{ attr }} >
  <div class="xuer-select_wrapper {{ className }}">
    <select 
      class="xuer-select xuer-focus" 
      name="{{ name }}" 
      id="{{ id }}"
      data-value="{{ value }}"
      xuer-select
    >
      {{ options }}
    </select>
    <label for="{{ id }}" class="xuer-select_label">{{ label | t }}</label>
    {%- render 'icon', name: 'dawn' -%}
</div>
</xuer-form-select>
